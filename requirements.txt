# Runtime dependencies for AdaMEM's training-free inference and index builders.
# Prefer `pip install -e .` so the project itself is installed as well.
hnswlib>=0.8.0
httpx>=0.27.0
numpy>=1.24.0
omegaconf>=2.3.0
openai>=1.30.0
python-dotenv>=1.0.0
PyYAML>=6.0
ray[default]>=2.41.0,<2.50.0
requests>=2.31.0
torch>=2.2.0
tqdm>=4.66.0
transformers>=4.45.0,<5.0.0
